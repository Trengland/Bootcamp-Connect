
<div class="col-md-3">
	<div class="card">
		<img alt="..." class="card-img-top" src="/images/profile.jpg">
		<div class="card-body">
			<h5 class="card-title">{{user.name}}</h5>
			<p class="card-text">{{birth_day}}</p>
            <p class="card-text">{{favorite_quote}}</p>
            <a class="btn btn-primary peer-profile" data-user-id={{user.id}}>View Profile</a>
		</div>
	</div>
</div>
