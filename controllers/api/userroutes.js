const router = require('express').Router();

const { Users } = require('../../models');

router.post('/', (req, res) => {
    //create new user
})

router.post('/login', (req, res) => {
    //login existing user
})

router.post('/', (req, res) => {
    //log out
})

